import { Component, inject } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { MatDialogModule, MAT_DIALOG_DATA, MatDialogRef } from '@angular/material/dialog';import { MatButtonModule } from '@angular/material/button';import { MatIconModule } from '@angular/material/icon';import { MatFormFieldModule } from '@angular/material/form-field';import { MatInputModule } from '@angular/material/input';import { MatSelectModule } from '@angular/material/select';import { EmployeeUnavailability, CreateUnavailabilityRequest } from '../../../../core/models/unavailability.model';@Component({  selector: 'app-unavailability-dialog',  standalone: true,  imports: [    CommonModule, FormsModule, MatDialogModule, MatButtonModule, MatIconModule,    MatFormFieldModule, MatInputModule, MatSelectModule  ],  template: `    <div class="unavail-dialog">      <h2 mat-dialog-title>        <mat-icon>event_busy</mat-icon>        {{ data.record ? 'Edit Unavailability' : 'Add Unavailability' }}      </h2>      <mat-dialog-content>        <form #f="ngForm" (ngSubmit)="save()">          <div class="dialog-form">            <mat-form-field appearance="outline" class="full-width">              <mat-label>Reason</mat-label>              <mat-select [(ngModel)]="form.unavailabilityType" name="unavailabilityType" required>                <mat-option *ngFor="let r of reasons" [value]="r.value">{{ r.label }}</mat-option>              </mat-select>            </mat-form-field>            <div class="form-row">              <mat-form-field appearance="outline">                <mat-label>Start Date</mat-label>                <input matInput [type]="'date'" name="startDate" [(ngModel)]="form.startDate" required>              </mat-form-field>              <mat-form-field appearance="outline">                <mat-label>End Date (optional)</mat-label>                <input matInput [type]="'date'" name="endDate" [(ngModel)]="form.endDate">              </mat-form-field>            </div>            <div class="form-row">              <mat-form-field appearance="outline">                <mat-label>Start Time (optional)</mat-label>                <input matInput [type]="'time'" name="startTime" [(ngModel)]="form.startTime">              </mat-form-field>              <mat-form-field appearance="outline">                <mat-label>End Time (optional)</mat-label>                <input matInput [type]="'time'" name="endTime" [(ngModel)]="form.endTime">              </mat-form-field>            </div>            <mat-form-field appearance="outline" class="full-width">              <mat-label>Repeat</mat-label>              <mat-select [(ngModel)]="form.repeat" name="repeat">                <mat-option value="None">None</mat-option>                <mat-option value="Daily">Daily</mat-option>                <mat-option value="Weekly">Weekly</mat-option>              </mat-select>            </mat-form-field>            @if (errorMessage) {              <div class="dialog-error">                <mat-icon>error</mat-icon> {{ errorMessage }}              </div>            }          </div>        </form>      </mat-dialog-content>      <mat-dialog-actions align="end">        <button mat-stroked-button (click)="close()">Cancel</button>        <button mat-raised-button color="primary" [disabled]="saving" (click)="save()">          <mat-icon>save</mat-icon> {{ saving ? 'Saving...' : 'Save' }}        </button>      </mat-dialog-actions>    </div>  `,  styleUrls: ['./unavailability-dialog.component.scss']})export class UnavailabilityDialogComponent {  dialogRef = inject(MatDialogRef<UnavailabilityDialogComponent>);  data = inject(MAT_DIALOG_DATA) as { record?: EmployeeUnavailability };  saving = false;  errorMessage = '';  reasons = [    { value: 'AnnualLeave', label: 'Annual Leave' },    { value: 'SickLeave', label: 'Sick Leave' },    { value: 'Meeting', label: 'Meeting' },    { value: 'BusinessTravel', label: 'Business Travel' },    { value: 'Training', label: 'Training' },    { value: 'Personal', label: 'Personal' },    { value: 'Other', label: 'Other' }  ];  form: CreateUnavailabilityRequest = this.data.record    ? {        employeeId: this.data.record.employeeId,        unavailabilityType: this.data.record.unavailabilityType || 'Other',        startDate: this.data.record.startDate,        endDate: this.data.record.endDate || undefined,        startTime: this.toInputTime(this.data.record.startTime),        endTime: this.toInputTime(this.data.record.endTime),        repeat: this.data.record.repeat || 'None'      }    : {        employeeId: 0,        unavailabilityType: 'Other',        startDate: '',        repeat: 'None'      };  private toInputTime(value?: string): string | undefined {    if (!value) return undefined;    const m = value.match(/^(\d{2}):(\d{2})/);    return m ? `${m[1]}:${m[2]}` : value;  }  close(): void { this.dialogRef.close(undefined); }  save(): void {    this.errorMessage = '';    if (!this.form.startDate) {      this.errorMessage = 'Start Date is required.';      return;    }    if (this.form.endDate && this.form.endDate < this.form.startDate) {      this.errorMessage = 'End Date cannot be before Start Date.';      return;    }    const hasStartTime = !!this.form.startTime;    const hasEndTime = !!this.form.endTime;    if (hasStartTime !== hasEndTime) {      this.errorMessage = 'Provide both Start Time and End Time, or leave both empty.';      return;    }    if (hasStartTime && hasEndTime && this.form.endTime! <= this.form.startTime!) {      this.errorMessage = 'End Time must be after Start Time.';      return;    }    this.saving = true;    this.dialogRef.close(this.form);  }}